version: 2.1
jobs:
  run_tests:
    docker:
      - image: cimg/go:1.20.2
    steps:
      - run: echo "Say hello to GoLang Project!"
      - run: go version
      - checkout
      # - run:
      #   name: Install go mods
      #   command: |
      #     go mod download
      #     go version
      # - run:
      #   name: Run unit test
      #   command: |
      #     go test

workflows:
  first_circle_ci:
    jobs:
      - run_tests
